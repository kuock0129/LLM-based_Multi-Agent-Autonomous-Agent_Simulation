#include <iostream>
#include <math.h>
#include "agent.h"

using namespace enviro;

// Note : All implementation is in Wanderer.h, but the Makefile
//        needs a .cc file to know what .so files to make